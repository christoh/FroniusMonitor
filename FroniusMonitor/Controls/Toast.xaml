<ContentControl x:Class="De.Hochstaetter.FroniusMonitor.Controls.Toast"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:De.Hochstaetter.FroniusMonitor.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="400">
    <Border Background="{Binding Background}" CornerRadius="{Binding CornerRadius}" DataContext="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=local:Toast}}">
        <Viewbox Margin="{Binding Padding}">
            <TextBlock Text="{Binding Text}" HorizontalAlignment="Center" VerticalAlignment="Center" TextAlignment="Center"/>
        </Viewbox>
    </Border>
</ContentControl>
