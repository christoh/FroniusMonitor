<Window x:Class="De.Hochstaetter.FroniusMonitor.Views.PriceComponentsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:De.Hochstaetter.FroniusMonitor.Views"
        xmlns:p="clr-namespace:De.Hochstaetter.Fronius.Localization;assembly=Fronius"
        mc:Ignorable="d" DataContext="{Binding RelativeSource={RelativeSource Self}}"
        Title="PriceComponentsView" SizeToContent="WidthAndHeight" ResizeMode="CanMinimize">
    <DataGrid IsReadOnly="True" GridLinesVisibility="Vertical" AlternatingRowBackground="AntiqueWhite" BorderThickness="0" ItemsSource="{Binding PriceComponents}" AutoGenerateColumns="False">
        <DataGrid.Resources>
            <Style TargetType="FrameworkElement" x:Key="Es">
                <Setter Property="HorizontalAlignment" Value="Stretch"/>
            </Style>
            
            <Style x:Key="RightAlign" TargetType="TextBlock">
                <Setter Property="TextAlignment" Value="Right"/>
                <Setter Property="Padding" Value="4,0"/>
            </Style>
            
        </DataGrid.Resources>
        <DataGrid.Columns>
            <DataGridTextColumn CanUserReorder="False" CanUserResize="False" Binding="{Binding Description}" Header="Component" HeaderStyle="{StaticResource Es}"/>
            <DataGridTextColumn CanUserReorder="False" CanUserResize="False" ElementStyle="{StaticResource RightAlign}" Binding="{Binding Price, StringFormat=N2}" Header="{x:Static p:Resources.Net}" HeaderStyle="{StaticResource Es}"/>
            <DataGridTextColumn CanUserReorder="False" CanUserResize="False" ElementStyle="{StaticResource RightAlign}" Binding="{Binding TaxRate, StringFormat=P1}" Header="V.A.T" HeaderStyle="{StaticResource Es}"/>
            <DataGridTextColumn CanUserReorder="False" CanUserResize="False" ElementStyle="{StaticResource RightAlign}" Binding="{Binding GrossPrice, StringFormat=N2}" Header="{x:Static p:Resources.Gross}"/>
        </DataGrid.Columns>
    </DataGrid>
</Window>
