<c:ScalableWindow x:Class="De.Hochstaetter.FroniusMonitor.Views.InverterDetailsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModels="clr-namespace:De.Hochstaetter.FroniusMonitor.ViewModels"
        xmlns:p="clr-namespace:De.Hochstaetter.Fronius.Localization;assembly=Fronius"
        xmlns:c="clr-namespace:De.Hochstaetter.FroniusMonitor.Controls"
        xmlns:m="clr-namespace:De.Hochstaetter.FroniusMonitor.Models"
        xmlns:co="clr-namespace:De.Hochstaetter.FroniusMonitor.Wpf.Converters"
        xmlns:t="clr-namespace:De.Hochstaetter.FroniusMonitor.Wpf.Resources"
        xmlns:l="clr-namespace:De.Hochstaetter.FroniusMonitor.Wpf.Localization"
        xmlns:v="clr-namespace:De.Hochstaetter.FroniusMonitor.Views"
        xmlns:a="clr-namespace:De.Hochstaetter.FroniusMonitor"
        xmlns:vi="clr-namespace:De.Hochstaetter.FroniusMonitor.Assets.Images"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=viewModels:InverterDetailsViewModel}"
        Title="{Binding Title}" Width="682" Height="979" MinHeight="200" MinWidth="300">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <Grid.ContextMenu>
            <ContextMenu>
                <MenuItem Header="{l:Resource Settings}" Icon="{vi:SettingsIcon}" Click="OnSettingsClicked" />
                <MenuItem Header="{l:Resource EnergyFlow}" Icon="{vi:EnergyFLow}" Click="OnEnergyFlowClicked" />
                <MenuItem Header="{l:Resource Modbus}" Icon="{vi:ModbusLogo}" Click="OnModbusClicked" />
                <MenuItem Header="{l:Resource EventLog}" Icon="{vi:EventLogIcon}" Click="OnEventLogClicked" />
            </ContextMenu>
        </Grid.ContextMenu>

        <Menu Grid.Row="0">
            <MenuItem Header="{l:Resource Inverter}">
                <MenuItem Header="{l:Resource Settings}" Icon="{vi:SettingsIcon}" Click="OnSettingsClicked" />
                <MenuItem Header="{l:Resource EnergyFlow}" Icon="{vi:EnergyFLow}" Click="OnEnergyFlowClicked" />
                <MenuItem Header="{l:Resource Modbus}" Icon="{vi:ModbusLogo}" Click="OnModbusClicked" />
                <MenuItem Header="{l:Resource EventLog}" Icon="{vi:EventLogIcon}" Click="OnEventLogClicked" />
            </MenuItem>
            <MenuItem x:Name="View" Header="{l:Resource View}" IsCheckable="False" IsChecked="False" Click="CheckShowAll">
                <MenuItem.Resources>
                    <Style TargetType="MenuItem">
                        <Setter Property="IsCheckable" Value="True" />
                        <Setter Property="IsChecked" Value="False" />
                    </Style>
                </MenuItem.Resources>
                <MenuItem Header="{l:Resource ShowAll}" IsCheckable="True" Click="CheckAllViews"/>
                <MenuItem x:Name="DcPower" Header="{l:Resource DcPower}" IsChecked="True"/>
                <MenuItem x:Name="DcVoltage" Header="{l:Resource DcVoltage}" IsChecked="True" />
                <MenuItem x:Name="DcCurrent" Header="{l:Resource DcCurrent}" IsChecked="True" />
                <MenuItem x:Name="Temperatures" Header="{l:Resource Temperatures}"/>
                <MenuItem x:Name="Fans" Header="{l:Resource Fans}"/>
                <MenuItem x:Name="AcPhaseVoltageFeedIn" Header="{l:Resource AcPhaseVoltageFeedIn}"/>
                <MenuItem x:Name="AcPhaseVoltageInverter" Header="{l:Resource AcPhaseVoltageInverter}"/>
                <MenuItem x:Name="ΔAcPhaseVoltageFeedIn" Header="{l:Resource ΔAcPhaseVoltageFeedIn}"/>
                <MenuItem x:Name="AcLineVoltageFeedIn" Header="{l:Resource AcLineVoltageFeedIn}"/>
                <MenuItem x:Name="AcLineVoltageInverter" Header="{l:Resource AcLineVoltageInverter}"/>
                <MenuItem x:Name="ΔAcLineVoltageFeedIn" Header="{l:Resource ΔAcLineVoltageFeedIn}"/>
                <MenuItem x:Name="Frequency" Header="{l:Resource Frequency}"/>
                <MenuItem x:Name="ActivePower" Header="{l:Resource ActivePower}" IsChecked="True"/>
                <MenuItem x:Name="ApparentPower" Header="{l:Resource ApparentPower}"/>
                <MenuItem x:Name="ReactivePower" Header="{l:Resource ReactivePower}"/>
                <MenuItem x:Name="PowerFactor" Header="cos(ϕ)"/>
                <MenuItem x:Name="Efficiency" Header="{l:Resource Efficiency}" IsChecked="True"/>
                <MenuItem x:Name="AcCurrent" Header="{l:Resource AcCurrent}"/>
                <MenuItem x:Name="OutOfBalance" Header="{l:Resource OutOfBalance}"/>
            </MenuItem>
        </Menu>

        <TextBlock Grid.Row="1" Text="{Binding Header}" TextAlignment="Center" FontSize="18" FontWeight="Medium" Margin="10,10,10,16" TextWrapping="Wrap" />

        <TextBlock Grid.Row="2" Text="{l:Resource CheckAtLeastOneView}" TextAlignment="Center" FontSize="14" Margin="10,10,10,16" TextWrapping="Wrap" Visibility="{Binding IsNoneSelected, Converter={co:Bool2Visibility}}" />

        <ScrollViewer Grid.Row="3" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
            <!-- ReSharper disable Xaml.PossibleNullReferenceException -->
            <WrapPanel Orientation="Horizontal" Margin="10,0,4,0">
                <WrapPanel.LayoutTransform>
                    <ScaleTransform x:Name="WrapScaler" ScaleX="1" ScaleY="1" />
                </WrapPanel.LayoutTransform>
                <WrapPanel.Resources>

                    <Style TargetType="c:Gauge" BasedOn="{StaticResource DefaultHalfCircleGauge}" x:Key="HalfCircleGauge">
                        <Setter Property="ColorAllTicks" Value="{Binding ColorAllGaugeTicks, Source={x:Static a:App.Settings}}" />
                    </Style>

                    <Style TargetType="ContentControl">
                        <Setter Property="ContentTemplate" Value="{StaticResource InverterGauge}" />
                    </Style>

                    <Style TargetType="GroupBox">
                        <Setter Property="FontSize" Value="16" />
                        <Setter Property="FontWeight" Value="Medium" />
                        <Setter Property="Margin" Value="4" />
                        <Setter Property="BorderThickness" Value="2" />
                        <Setter Property="BorderBrush" Value="Gray" />
                        <Setter Property="Padding" Value="4" />
                    </Style>

                    <Style TargetType="c:Gauge" x:Key="AcPowerGauge" BasedOn="{StaticResource HalfCircleGauge}">
                        <Setter Property="Minimum">
                            <Setter.Value>
                                <MultiBinding Converter="{co:AcPowerMinimum}">
                                    <Binding Path="Inverter.Config.MaxAcPower" Converter="{co:Multiply Factor=.33333333333333333333}" />
                                    <Binding Path="Inverter.Sensors.Storage" />
                                </MultiBinding>
                            </Setter.Value>
                        </Setter>
                        <Setter Property="Maximum" Value="{Binding Inverter.Config.MaxAcPower, Converter={co:Multiply Factor=.33333333333333333333}, FallbackValue=3333.333333333333}" />
                        <Setter Property="StringFormat" Value="N1" />
                        <Setter Property="GaugeColors" Value="{Binding Inverter.Sensors.Storage, FallbackValue={x:Null}, Converter={co:NullToColorMap NotNull={x:Static c:Gauge.MidIsGood}, Null={x:Static c:Gauge.HighIsBad}}}" />
                        <Setter Property="Origin" Value="{Binding Inverter.Sensors.Storage, Converter={co:NullToDouble Null=0, NotNull=.5}, FallbackValue=0}" />
                    </Style>
                </WrapPanel.Resources>

                <GroupBox Header="{l:Resource DcPower}" Visibility="{Binding IsChecked, ElementName=DcPower, Converter={co:Bool2Visibility}}">
                    <StackPanel>
                        <StackPanel.Resources>
                            <Style TargetType="c:Gauge" BasedOn="{StaticResource HalfCircleGauge}">
                                <Setter Property="StringFormat" Value="N1" />
                                <Setter Property="UnitName" Value="W" />
                                <Setter Property="GaugeColors" Value="{x:Static c:Gauge.HighIsBad}" />
                            </Style>
                        </StackPanel.Resources>
                        <StackPanel Orientation="Horizontal">
                            <ContentControl>
                                <c:Gauge Maximum="{m:GaugeBinding Inverter.Config.InverterSettings.Mppt.Mppt1.WattPeak, FallbackValue=5000}"
                                         Value="{m:GaugeBinding Inverter.Sensors.Inverter.Solar1Power}" Label="{l:Channel 'MPPT1'}" />
                            </ContentControl>

                            <ContentControl>
                                <c:Gauge Maximum="{m:GaugeBinding Inverter.Config.InverterSettings.Mppt.Mppt2.WattPeak, FallbackValue=5000}"
                                         Value="{m:GaugeBinding Inverter.Sensors.Inverter.Solar2Power}" Label="{l:Channel 'MPPT2'}" />
                            </ContentControl>

                            <ContentControl>
                                <c:Gauge Maximum="{m:GaugeBinding Inverter.Config.InverterSettings.Mppt.WattPeakTotal, FallbackValue=10000, TargetNullValue=10000}"
                                         Value="{m:GaugeBinding Inverter.Sensors.Inverter.SolarPowerSum}" Label="{l:Resource SumPv}" />
                            </ContentControl>

                            <ContentControl Visibility="{Binding Inverter.Sensors.Storage, Converter={co:NullToVisibility}}">
                                <c:Gauge Origin=".5" Value="{m:GaugeBinding Inverter.Sensors.Inverter.StoragePower}" Label="{l:Resource Battery}"
                                         Maximum="{Binding Inverter.Sensors.Storage.VoltageOuter, FallbackValue=8184, Converter={co:Multiply Factor=22}}"
                                         Minimum="{Binding Inverter.Sensors.Storage.VoltageOuter, FallbackValue=-8184, Converter={co:Multiply Factor=-22}}" />
                            </ContentControl>

                            <ContentControl>
                                <c:Gauge Minimum="50" Maximum="150" UnitName="%" GaugeColors="{x:Static c:Gauge.ExtremeIsBad}" Label="{l:Resource MpptComparison}">
                                    <c:Gauge.Value>
                                        <MultiBinding Converter="{co:MpptComparison}">
                                            <Binding Path="Inverter.Sensors.Inverter.Solar1Power" />
                                            <Binding Path="Inverter.Sensors.Inverter.Solar2Power" />
                                            <Binding Path="Inverter.Config.InverterSettings.Mppt.Mppt1.WattPeak"/>
                                            <Binding Path="Inverter.Config.InverterSettings.Mppt.Mppt2.WattPeak"/>
                                        </MultiBinding>
                                    </c:Gauge.Value>
                                </c:Gauge>
                            </ContentControl>
                        </StackPanel>
                    </StackPanel>
                </GroupBox>

                <GroupBox Header="{l:Resource DcVoltage}" Visibility="{Binding IsChecked, ElementName=DcVoltage, Converter={co:Bool2Visibility}}">
                    <StackPanel>
                        <StackPanel.Resources>
                            <Style TargetType="c:Gauge" BasedOn="{StaticResource HalfCircleGauge}">
                                <Setter Property="StringFormat" Value="N1" />
                                <Setter Property="UnitName" Value="V" />
                                <Setter Property="Minimum" Value="80" />
                                <Setter Property="Maximum" Value="800" />
                                <Setter Property="GaugeColors" Value="{x:Static c:Gauge.HighIsBad}" />
                                <Setter Property="v:InverterDetailsView.AlwaysUseRunningBackground" Value="True" />
                            </Style>
                        </StackPanel.Resources>

                        <StackPanel Orientation="Horizontal">

                            <ContentControl>
                                <c:Gauge Value="{m:GaugeBinding Inverter.Sensors.Inverter.Solar1Voltage}" Label="{l:Channel 'MPPT1'}" />
                            </ContentControl>

                            <ContentControl>
                                <c:Gauge Value="{m:GaugeBinding Inverter.Sensors.Inverter.Solar2Voltage}" Label="{l:Channel 'MPPT2'}" />
                            </ContentControl>

                            <ContentControl>
                                <c:Gauge Value="{m:GaugeBinding Inverter.Sensors.Inverter.DcLinkVoltage}" Label="Link"
                                         Minimum="400" Maximum="800" GaugeColors="{x:Static c:Gauge.LowIsBad}" />
                            </ContentControl>

                            <ContentControl>
                                <c:Gauge Value="{m:GaugeBinding Inverter.Sensors.Inverter.StorageVoltageOuter}" Label="{l:Resource Battery}"
                                         Minimum="{m:GaugeBinding Inverter.Sensors.Storage.MinVoltage, FallbackValue=160}"
                                         Maximum="{m:GaugeBinding Inverter.Sensors.Storage.MaxVoltage, FallbackValue=531}"
                                         GaugeColors="{x:Static c:Gauge.ExtremeIsBad}" />
                            </ContentControl>
                        </StackPanel>
                    </StackPanel>
                </GroupBox>

                <GroupBox Header="{l:Resource DcCurrent}" Visibility="{Binding IsChecked, ElementName=DcCurrent, Converter={co:Bool2Visibility}}">
                    <StackPanel>
                        <StackPanel.Resources>
                            <Style TargetType="c:Gauge" BasedOn="{StaticResource HalfCircleGauge}">
                                <Setter Property="StringFormat" Value="N3" />
                                <Setter Property="UnitName" Value="A" />
                                <Setter Property="GaugeColors" Value="{x:Static c:Gauge.HighIsBad}" />
                                <Setter Property="t:HalfCircleGauge.MinimumMaximumStringFormat" Value="N1" />
                            </Style>
                        </StackPanel.Resources>
                        <StackPanel Orientation="Horizontal">
                            <ContentControl>
                                <c:Gauge Maximum="25" Value="{m:GaugeBinding Inverter.Sensors.Inverter.Solar1Current}" Label="{l:Channel 'MPPT1'}" />
                            </ContentControl>

                            <ContentControl>
                                <c:Gauge Maximum="12.5" Value="{m:GaugeBinding Inverter.Sensors.Inverter.Solar2Current}" Label="{l:Channel 'MPPT2'}" />
                            </ContentControl>

                            <ContentControl>
                                <c:Gauge Maximum="37.5" Value="{m:GaugeBinding Inverter.Sensors.Inverter.SolarCurrentSum}" Label="{l:Resource SumPv}" />
                            </ContentControl>

                            <ContentControl Visibility="{Binding Inverter.Sensors.Storage, Converter={co:NullToVisibility}}">
                                <c:Gauge Maximum="22" Minimum="-22" Origin=".5" Value="{m:GaugeBinding Inverter.Sensors.Inverter.StorageCurrent}" Label="{l:Resource Battery}" />
                            </ContentControl>

                            <ContentControl>
                                <c:Gauge Minimum="50" Maximum="150" UnitName="%" GaugeColors="{x:Static c:Gauge.ExtremeIsBad}" Label="{l:Resource MpptComparison}">
                                    <c:Gauge.Value>
                                        <MultiBinding Converter="{co:MpptComparison}">
                                            <Binding Path="Inverter.Sensors.Inverter.Solar1Current" />
                                            <Binding Path="Inverter.Sensors.Inverter.Solar2Current" />
                                        </MultiBinding>
                                    </c:Gauge.Value>
                                </c:Gauge>
                            </ContentControl>

                        </StackPanel>
                    </StackPanel>
                </GroupBox>

                <GroupBox Header="{l:Resource Temperatures}" Visibility="{Binding IsChecked, ElementName=Temperatures, Converter={co:Bool2Visibility}}">
                    <StackPanel>
                        <StackPanel.Resources>
                            <Style TargetType="c:Gauge" BasedOn="{StaticResource HalfCircleGauge}">
                                <Setter Property="UnitName" Value="°C" />
                                <Setter Property="GaugeColors" Value="{x:Static c:Gauge.HighIsBad}" />
                                <Setter Property="Minimum" Value="0" />
                                <Setter Property="Maximum" Value="100" />
                                <Setter Property="v:InverterDetailsView.AlwaysUseRunningBackground" Value="True" />
                            </Style>
                        </StackPanel.Resources>
                        <StackPanel Orientation="Horizontal">
                            <ContentControl>
                                <c:Gauge Value="{m:GaugeBinding Inverter.Sensors.Inverter.AmbientTemperature}" Label="{l:Resource Ambient}" />
                            </ContentControl>

                            <ContentControl>
                                <c:Gauge Value="{m:GaugeBinding Inverter.Sensors.Inverter.TemperatureInverterAcModule}" Label="{l:Resource AcModule}" />
                            </ContentControl>

                            <ContentControl>
                                <c:Gauge Value="{m:GaugeBinding Inverter.Sensors.Inverter.TemperatureInverterDcModule}" Label="{l:Resource DcModule}" />
                            </ContentControl>

                            <ContentControl>
                                <c:Gauge Value="{m:GaugeBinding Inverter.Sensors.Inverter.TemperatureInverterBatteryModule}" Label="{l:Resource BatteryModule}" />
                            </ContentControl>
                        </StackPanel>
                    </StackPanel>
                </GroupBox>

                <GroupBox Header="{l:Resource Fans}" Visibility="{Binding IsChecked, ElementName=Fans, Converter={co:Bool2Visibility}}">
                    <StackPanel>
                        <StackPanel.Resources>
                            <Style TargetType="c:Gauge" BasedOn="{StaticResource HalfCircleGauge}">
                                <Setter Property="UnitName" Value="%" />
                                <Setter Property="GaugeColors" Value="{x:Static c:Gauge.HighIsBad}" />
                                <Setter Property="Minimum" Value="0" />
                                <Setter Property="Maximum" Value="100" />
                                <Setter Property="v:InverterDetailsView.AlwaysUseRunningBackground" Value="True" />
                            </Style>
                        </StackPanel.Resources>
                        <StackPanel Orientation="Horizontal">
                            <ContentControl>
                                <c:Gauge Value="{m:GaugeBinding Inverter.Sensors.Inverter.Fan1RelativeRpm, Converter={co:Multiply Factor=100}}" Label="{l:Resource Fan1}" />
                            </ContentControl>

                            <ContentControl>
                                <c:Gauge Value="{m:GaugeBinding Inverter.Sensors.Inverter.Fan2RelativeRpm, Converter={co:Multiply Factor=100}}" Label="{l:Resource Fan2}" />
                            </ContentControl>
                        </StackPanel>
                    </StackPanel>
                </GroupBox>

                <GroupBox Header="{l:Resource AcPhaseVoltageFeedIn}" Visibility="{Binding IsChecked, ElementName=AcPhaseVoltageFeedIn, Converter={co:Bool2Visibility}}">
                    <StackPanel>
                        <StackPanel.Resources>
                            <Style TargetType="c:Gauge" BasedOn="{StaticResource HalfCircleGauge}">
                                <Setter Property="UnitName" Value="V" />
                                <Setter Property="Minimum" Value="207" />
                                <Setter Property="Maximum" Value="253" />
                                <Setter Property="Origin" Value=".5" />
                                <Setter Property="GaugeColors" Value="{x:Static c:Gauge.MidIsGood}" />
                                <Setter Property="v:InverterDetailsView.AlwaysUseRunningBackground" Value="True" />
                            </Style>
                        </StackPanel.Resources>
                        <StackPanel Orientation="Horizontal">
                            <ContentControl>
                                <c:Gauge Value="{m:GaugeBinding Inverter.Sensors.Inverter.FeedInPointPhaseVoltageL1}" Label="L1" />
                            </ContentControl>

                            <ContentControl>
                                <c:Gauge Value="{m:GaugeBinding Inverter.Sensors.Inverter.FeedInPointPhaseVoltageL2}" Label="L2" />
                            </ContentControl>

                            <ContentControl>
                                <c:Gauge Value="{m:GaugeBinding Inverter.Sensors.Inverter.FeedInPointPhaseVoltageL3}" Label="L3" />
                            </ContentControl>
                        </StackPanel>
                    </StackPanel>
                </GroupBox>

                <GroupBox Header="{l:Resource AcPhaseVoltageInverter}" Visibility="{Binding IsChecked, ElementName=AcPhaseVoltageInverter, Converter={co:Bool2Visibility}}">
                    <StackPanel>
                        <StackPanel.Resources>
                            <Style TargetType="c:Gauge" BasedOn="{StaticResource HalfCircleGauge}">
                                <Setter Property="UnitName" Value="V" />
                                <Setter Property="Minimum" Value="207" />
                                <Setter Property="Maximum" Value="253" />
                                <Setter Property="Origin" Value=".5" />
                                <Setter Property="GaugeColors" Value="{x:Static c:Gauge.MidIsGood}" />
                            </Style>
                        </StackPanel.Resources>
                        <StackPanel Orientation="Horizontal">
                            <ContentControl>
                                <c:Gauge Value="{m:GaugeBinding Inverter.Sensors.Inverter.AcVoltageL1}" Label="L1" />
                            </ContentControl>

                            <ContentControl>
                                <c:Gauge Value="{m:GaugeBinding Inverter.Sensors.Inverter.AcVoltageL2}" Label="L2" />
                            </ContentControl>

                            <ContentControl>
                                <c:Gauge Value="{m:GaugeBinding Inverter.Sensors.Inverter.AcVoltageL3}" Label="L3" />
                            </ContentControl>
                        </StackPanel>
                    </StackPanel>
                </GroupBox>

                <GroupBox Header="{l:Resource ΔAcPhaseVoltageFeedIn}" Visibility="{Binding IsChecked, ElementName=ΔAcPhaseVoltageFeedIn, Converter={co:Bool2Visibility}}">
                    <StackPanel>
                        <StackPanel.Resources>
                            <Style TargetType="c:Gauge" BasedOn="{StaticResource HalfCircleGauge}">
                                <Setter Property="StringFormat" Value="N2" />
                                <Setter Property="UnitName" Value="V" />
                                <Setter Property="Minimum" Value="-0.7" />
                                <Setter Property="Maximum" Value="0.7" />
                                <Setter Property="Origin" Value=".5" />
                                <Setter Property="GaugeColors" Value="{x:Static c:Gauge.MidIsGood}" />
                                <Setter Property="t:HalfCircleGauge.MinimumMaximumStringFormat" Value="N1" />
                            </Style>
                        </StackPanel.Resources>
                        <StackPanel Orientation="Horizontal">
                            <ContentControl>
                                <c:Gauge Label="L1">
                                    <c:Gauge.Value>
                                        <MultiBinding Converter="{co:DeltaConverter}">
                                            <Binding Path="Inverter.Sensors.Inverter.AcVoltageL1" />
                                            <Binding Path="Inverter.Sensors.Inverter.FeedInPointPhaseVoltageL1" />
                                        </MultiBinding>
                                    </c:Gauge.Value>
                                </c:Gauge>
                            </ContentControl>

                            <ContentControl>
                                <c:Gauge Label="L2">
                                    <c:Gauge.Value>
                                        <MultiBinding Converter="{co:DeltaConverter}">
                                            <Binding Path="Inverter.Sensors.Inverter.AcVoltageL2" />
                                            <Binding Path="Inverter.Sensors.Inverter.FeedInPointPhaseVoltageL2" />
                                        </MultiBinding>
                                    </c:Gauge.Value>
                                </c:Gauge>
                            </ContentControl>

                            <ContentControl>
                                <c:Gauge Label="L3">
                                    <c:Gauge.Value>
                                        <MultiBinding Converter="{co:DeltaConverter}">
                                            <Binding Path="Inverter.Sensors.Inverter.AcVoltageL3" />
                                            <Binding Path="Inverter.Sensors.Inverter.FeedInPointPhaseVoltageL3" />
                                        </MultiBinding>
                                    </c:Gauge.Value>
                                </c:Gauge>
                            </ContentControl>
                        </StackPanel>
                    </StackPanel>
                </GroupBox>

                <GroupBox Header="{l:Resource AcLineVoltageFeedIn}" Visibility="{Binding IsChecked, ElementName=AcLineVoltageFeedIn, Converter={co:Bool2Visibility}}">
                    <StackPanel>
                        <StackPanel.Resources>
                            <Style TargetType="c:Gauge" BasedOn="{StaticResource HalfCircleGauge}">
                                <Setter Property="UnitName" Value="V" />
                                <Setter Property="Minimum" Value="358.5345171668" />
                                <Setter Property="Maximum" Value="438.2088543149" />
                                <Setter Property="Origin" Value=".5" />
                                <Setter Property="GaugeColors" Value="{x:Static c:Gauge.MidIsGood}" />
                                <Setter Property="v:InverterDetailsView.AlwaysUseRunningBackground" Value="True" />
                            </Style>
                        </StackPanel.Resources>
                        <StackPanel Orientation="Horizontal">
                            <ContentControl>
                                <c:Gauge Value="{m:GaugeBinding Inverter.Sensors.Inverter.FeedInPointLineVoltageL12}" Label="L12" />
                            </ContentControl>

                            <ContentControl>
                                <c:Gauge Value="{m:GaugeBinding Inverter.Sensors.Inverter.FeedInPointLineVoltageL23}" Label="L23" />
                            </ContentControl>

                            <ContentControl>
                                <c:Gauge Value="{m:GaugeBinding Inverter.Sensors.Inverter.FeedInPointLineVoltageL31}" Label="L31" />
                            </ContentControl>
                        </StackPanel>
                    </StackPanel>
                </GroupBox>

                <GroupBox Header="{l:Resource AcLineVoltageInverter}" Visibility="{Binding IsChecked, ElementName=AcLineVoltageInverter, Converter={co:Bool2Visibility}}">
                    <StackPanel>
                        <StackPanel.Resources>
                            <Style TargetType="c:Gauge" BasedOn="{StaticResource HalfCircleGauge}">
                                <Setter Property="UnitName" Value="V" />
                                <Setter Property="Minimum" Value="358.5345171668" />
                                <Setter Property="Maximum" Value="438.2088543149" />
                                <Setter Property="Origin" Value=".5" />
                                <Setter Property="GaugeColors" Value="{x:Static c:Gauge.MidIsGood}" />
                            </Style>
                        </StackPanel.Resources>
                        <StackPanel Orientation="Horizontal">
                            <ContentControl>
                                <c:Gauge Value="{m:GaugeBinding Inverter.Sensors.Inverter.AcVoltageL12}" Label="L12" />
                            </ContentControl>

                            <ContentControl>
                                <c:Gauge Value="{m:GaugeBinding Inverter.Sensors.Inverter.AcVoltageL23}" Label="L23" />
                            </ContentControl>

                            <ContentControl>
                                <c:Gauge Value="{m:GaugeBinding Inverter.Sensors.Inverter.AcVoltageL31}" Label="L31" />
                            </ContentControl>
                        </StackPanel>
                    </StackPanel>
                </GroupBox>

                <GroupBox Header="{l:Resource ΔAcLineVoltageFeedIn}" Visibility="{Binding IsChecked, ElementName=ΔAcLineVoltageFeedIn, Converter={co:Bool2Visibility}}">
                    <StackPanel>
                        <StackPanel.Resources>
                            <Style TargetType="c:Gauge" BasedOn="{StaticResource HalfCircleGauge}">
                                <Setter Property="StringFormat" Value="N2" />
                                <Setter Property="UnitName" Value="V" />
                                <Setter Property="Minimum" Value="-1" />
                                <Setter Property="Maximum" Value="1" />
                                <Setter Property="Origin" Value=".5" />
                                <Setter Property="GaugeColors" Value="{x:Static c:Gauge.MidIsGood}" />
                            </Style>
                        </StackPanel.Resources>
                        <StackPanel Orientation="Horizontal">
                            <ContentControl>
                                <c:Gauge Label="L12">
                                    <c:Gauge.Value>
                                        <MultiBinding Converter="{co:DeltaConverter}">
                                            <Binding Path="Inverter.Sensors.Inverter.AcVoltageL12" />
                                            <Binding Path="Inverter.Sensors.Inverter.FeedInPointLineVoltageL12" />
                                        </MultiBinding>
                                    </c:Gauge.Value>
                                </c:Gauge>
                            </ContentControl>

                            <ContentControl>
                                <c:Gauge Label="L23">
                                    <c:Gauge.Value>
                                        <MultiBinding Converter="{co:DeltaConverter}">
                                            <Binding Path="Inverter.Sensors.Inverter.AcVoltageL23" />
                                            <Binding Path="Inverter.Sensors.Inverter.FeedInPointLineVoltageL23" />
                                        </MultiBinding>
                                    </c:Gauge.Value>
                                </c:Gauge>
                            </ContentControl>

                            <ContentControl>
                                <c:Gauge Label="L31">
                                    <c:Gauge.Value>
                                        <MultiBinding Converter="{co:DeltaConverter}">
                                            <Binding Path="Inverter.Sensors.Inverter.AcVoltageL31" />
                                            <Binding Path="Inverter.Sensors.Inverter.FeedInPointLineVoltageL31" />
                                        </MultiBinding>
                                    </c:Gauge.Value>
                                </c:Gauge>
                            </ContentControl>
                        </StackPanel>
                    </StackPanel>
                </GroupBox>

                <GroupBox Header="{l:Resource Frequency}" Visibility="{Binding IsChecked, ElementName=Frequency, Converter={co:Bool2Visibility}}">
                    <StackPanel>
                        <StackPanel.Resources>
                            <Style TargetType="c:Gauge" BasedOn="{StaticResource HalfCircleGauge}">
                                <Setter Property="StringFormat" Value="N3" />
                                <Setter Property="UnitName" Value="Hz" />
                                <Setter Property="Minimum" Value="49.8" />
                                <Setter Property="Maximum" Value="50.2" />
                                <Setter Property="Origin" Value=".5" />
                                <Setter Property="GaugeColors" Value="{x:Static c:Gauge.MidIsGood}" />
                            </Style>
                        </StackPanel.Resources>
                        <StackPanel Orientation="Horizontal">
                            <ContentControl>
                                <c:Gauge Value="{m:GaugeBinding Inverter.Sensors.Inverter.FeedInPointFrequency}" Label="{l:Resource Grid}"
                                         v:InverterDetailsView.AlwaysUseRunningBackground="True" t:HalfCircleGauge.MinimumMaximumStringFormat="N1" />
                            </ContentControl>

                            <ContentControl>
                                <c:Gauge Value="{m:GaugeBinding Inverter.Sensors.Inverter.InverterFrequency}" Label="{l:Resource Inverter}"
                                         t:HalfCircleGauge.MinimumMaximumStringFormat="N1" />
                            </ContentControl>

                            <ContentControl>
                                <c:Gauge Label="{l:Resource ΔFrequency}" Minimum="-5" Maximum="5" UnitName="mHz">
                                    <c:Gauge.Value>
                                        <MultiBinding Converter="{co:DeltaConverter}">
                                            <Binding Path="Inverter.Sensors.Inverter.InverterFrequency" Converter="{co:Multiply Factor=1000}" />
                                            <Binding Path="Inverter.Sensors.Inverter.FeedInPointFrequency" Converter="{co:Multiply Factor=1000}" />
                                        </MultiBinding>
                                    </c:Gauge.Value>
                                </c:Gauge>
                            </ContentControl>
                        </StackPanel>
                    </StackPanel>
                </GroupBox>

                <GroupBox Header="{l:Resource ActivePower}" Visibility="{Binding IsChecked, ElementName=ActivePower, Converter={co:Bool2Visibility}}">
                    <StackPanel>
                        <StackPanel.Resources>
                            <Style TargetType="c:Gauge" BasedOn="{StaticResource AcPowerGauge}">
                                <Setter Property="UnitName" Value="W" />
                            </Style>
                        </StackPanel.Resources>
                        <StackPanel Orientation="Horizontal">
                            <ContentControl>
                                <c:Gauge Value="{m:GaugeBinding Inverter.Sensors.Inverter.ActivePowerL1}" Label="L1" />
                            </ContentControl>

                            <ContentControl>
                                <c:Gauge Value="{m:GaugeBinding Inverter.Sensors.Inverter.ActivePowerL2}" Label="L2" />
                            </ContentControl>

                            <ContentControl>
                                <c:Gauge Value="{m:GaugeBinding Inverter.Sensors.Inverter.ActivePowerL3}" Label="L3" />
                            </ContentControl>

                            <ContentControl>
                                <c:Gauge Value="{Binding Inverter.Sensors.Inverter.PowerActiveSum, FallbackValue=-100000, TargetNullValue=-100000, Mode=OneWay}"
                                         Maximum="{Binding Inverter.Config.MaxAcPower, FallbackValue=10000, TargetNullValue=10000, Mode=OneWay}"
                                         Label="{l:Resource Sum}">
                                    <c:Gauge.Minimum>
                                        <MultiBinding Converter="{co:AcPowerMinimum}">
                                            <Binding Path="Inverter.Config.MaxAcPower" />
                                            <Binding Path="Inverter.Sensors.Storage" />
                                        </MultiBinding>
                                    </c:Gauge.Minimum>
                                </c:Gauge>
                            </ContentControl>
                        </StackPanel>
                    </StackPanel>
                </GroupBox>

                <GroupBox Header="{l:Resource ApparentPower}" Visibility="{Binding IsChecked, ElementName=ApparentPower, Converter={co:Bool2Visibility}}">
                    <StackPanel>
                        <StackPanel.Resources>
                            <Style TargetType="c:Gauge" BasedOn="{StaticResource AcPowerGauge}">
                                <Setter Property="UnitName" Value="VA" />
                                <Setter Property="Minimum" Value="0" />
                                <Setter Property="Origin" Value="0" />
                                <Setter Property="GaugeColors" Value="{x:Static c:Gauge.HighIsBad}" />
                            </Style>
                        </StackPanel.Resources>
                        <StackPanel Orientation="Horizontal">
                            <ContentControl>
                                <c:Gauge Value="{m:GaugeBinding Inverter.Sensors.Inverter.ApparentPowerL1}" Label="L1" />
                            </ContentControl>

                            <ContentControl>
                                <c:Gauge Value="{m:GaugeBinding Inverter.Sensors.Inverter.ApparentPowerL2}" Label="L2" />
                            </ContentControl>

                            <ContentControl>
                                <c:Gauge Value="{m:GaugeBinding Inverter.Sensors.Inverter.ApparentPowerL3}" Label="L3" />
                            </ContentControl>

                            <ContentControl>
                                <c:Gauge Value="{Binding Inverter.Sensors.Inverter.PowerApparentSum, FallbackValue=-100000, TargetNullValue=-100000, Mode=OneWay}"
                                         Maximum="{Binding Inverter.Config.MaxAcPower, FallbackValue=10000, TargetNullValue=10000, Mode=OneWay}"
                                         Label="{l:Resource Sum}" />
                            </ContentControl>
                        </StackPanel>
                    </StackPanel>
                </GroupBox>

                <GroupBox Header="{l:Resource ReactivePower}" Visibility="{Binding IsChecked, ElementName=ReactivePower, Converter={co:Bool2Visibility}}">
                    <StackPanel>
                        <StackPanel.Resources>
                            <Style TargetType="c:Gauge" BasedOn="{StaticResource AcPowerGauge}">
                                <Setter Property="UnitName" Value="VA(r)" />
                                <Setter Property="Origin" Value=".5" />
                                <Setter Property="Minimum" Value="{Binding Inverter.Config.MaxAcPower, FallbackValue=-10000, TargetNullValue=-10000, Mode=OneWay, Converter={co:Multiply Factor=-0.3333333333333}}" />
                            </Style>
                        </StackPanel.Resources>
                        <StackPanel Orientation="Horizontal">
                            <ContentControl>
                                <c:Gauge Value="{m:GaugeBinding Inverter.Sensors.Inverter.ReactivePowerL1}" Label="L1" />
                            </ContentControl>

                            <ContentControl>
                                <c:Gauge Value="{m:GaugeBinding Inverter.Sensors.Inverter.ReactivePowerL2}" Label="L2" />
                            </ContentControl>

                            <ContentControl>
                                <c:Gauge Value="{m:GaugeBinding Inverter.Sensors.Inverter.ReactivePowerL3}" Label="L3" />
                            </ContentControl>

                            <ContentControl>
                                <c:Gauge Value="{Binding Inverter.Sensors.Inverter.PowerReactiveSum, FallbackValue=-100000, TargetNullValue=-100000, Mode=OneWay}"
                                         Minimum="{Binding Inverter.Config.MaxAcPower, FallbackValue=-10000, TargetNullValue=-10000, Mode=OneWay, Converter={co:Multiply Factor=-1}}"
                                         Maximum="{Binding Inverter.Config.MaxAcPower, FallbackValue=10000, TargetNullValue=10000, Mode=OneWay}"
                                         Label="{l:Resource Sum}" />
                            </ContentControl>
                        </StackPanel>
                    </StackPanel>
                </GroupBox>

                <GroupBox Header="cos(ϕ)" Visibility="{Binding IsChecked, ElementName=PowerFactor, Converter={co:Bool2Visibility}}">
                    <StackPanel>
                        <StackPanel.Resources>
                            <Style TargetType="c:Gauge" BasedOn="{StaticResource HalfCircleGauge}">
                                <Setter Property="Minimum" Value="-1" />
                                <Setter Property="Maximum" Value="1" />
                                <Setter Property="Origin" Value=".5" />
                                <Setter Property="StringFormat" Value="N5" />
                                <Setter Property="GaugeColors" Value="{x:Static c:Gauge.MidIsBad}" />
                            </Style>
                        </StackPanel.Resources>
                        <StackPanel Orientation="Horizontal">
                            <ContentControl>
                                <c:Gauge Value="{m:GaugeBinding Inverter.Sensors.Inverter.PowerFactorL1}" Label="L1" />
                            </ContentControl>

                            <ContentControl>
                                <c:Gauge Value="{m:GaugeBinding Inverter.Sensors.Inverter.PowerFactorL2}" Label="L2" />
                            </ContentControl>

                            <ContentControl>
                                <c:Gauge Value="{m:GaugeBinding Inverter.Sensors.Inverter.PowerFactorL3}" Label="L3" />
                            </ContentControl>

                            <ContentControl>
                                <c:Gauge Value="{m:GaugeBinding Inverter.Sensors.Inverter.PowerFactorAverage}" Label="{l:Resource Total}" />
                            </ContentControl>
                        </StackPanel>
                    </StackPanel>
                </GroupBox>

                <GroupBox Header="{l:Resource Efficiency}" Visibility="{Binding IsChecked, ElementName=Efficiency, Converter={co:Bool2Visibility}}">
                    <StackPanel>
                        <StackPanel.Resources>
                            <Style TargetType="c:Gauge" BasedOn="{StaticResource HalfCircleGauge}" />
                        </StackPanel.Resources>
                        <StackPanel Orientation="Horizontal">
                            <ContentControl>
                                <c:Gauge Value="{m:GaugeBinding Inverter.Sensors.PowerFlow.PowerLoss}" Label="{l:Resource Loss}"
                                         UnitName="W" Minimum="0" Maximum="500" GaugeColors="{x:Static c:Gauge.HighIsBad}" />
                            </ContentControl>

                            <ContentControl>
                                <c:Gauge Value="{m:GaugeBinding Inverter.Sensors.PowerFlow.Efficiency, Converter={co:Multiply Factor=100}}"
                                         Label="{l:Resource Efficiency}"
                                         UnitName="%" Minimum="0" Maximum="100" GaugeColors="{x:Static c:Gauge.LowIsBad}" />
                            </ContentControl>
                        </StackPanel>
                    </StackPanel>
                </GroupBox>

                <GroupBox Header="{l:Resource AcCurrent}" Visibility="{Binding IsChecked, ElementName=AcCurrent, Converter={co:Bool2Visibility}}">
                    <StackPanel>
                        <StackPanel.Resources>
                            <Style TargetType="c:Gauge" BasedOn="{StaticResource HalfCircleGauge}">
                                <Setter Property="Maximum" Value="{Binding Inverter.Config.MaxAcPower, Converter={co:Multiply Factor=0.00144928}, FallbackValue=14.4928}" />
                                <Setter Property="UnitName" Value="A" />
                                <Setter Property="GaugeColors" Value="{x:Static c:Gauge.HighIsBad}" />
                                <Setter Property="StringFormat" Value="N3" />
                                <Setter Property="t:HalfCircleGauge.MinimumMaximumStringFormat" Value="N1" />
                            </Style>
                        </StackPanel.Resources>
                        <StackPanel Orientation="Horizontal">
                            <ContentControl>
                                <c:Gauge Value="{m:GaugeBinding Inverter.Sensors.Inverter.CurrentL1}" Label="L1" />
                            </ContentControl>

                            <ContentControl>
                                <c:Gauge Value="{m:GaugeBinding Inverter.Sensors.Inverter.CurrentL2}" Label="L2" />
                            </ContentControl>

                            <ContentControl>
                                <c:Gauge Value="{m:GaugeBinding Inverter.Sensors.Inverter.CurrentL3}" Label="L3" />
                            </ContentControl>

                            <ContentControl>
                                <c:Gauge Value="{m:GaugeBinding Inverter.Sensors.Inverter.CurrentSum}" Label="{l:Resource Sum}"
                                         Maximum="{Binding Inverter.Config.MaxAcPower, Converter={co:Multiply Factor=0.00434784}, FallbackValue=14.4928}" />
                            </ContentControl>
                        </StackPanel>
                    </StackPanel>
                </GroupBox>

                <GroupBox Header="{l:Resource OutOfBalance}" Visibility="{Binding IsChecked, ElementName=OutOfBalance, Converter={co:Bool2Visibility}}">
                    <StackPanel>
                        <StackPanel.Resources>
                            <Style TargetType="c:Gauge" BasedOn="{StaticResource HalfCircleGauge}">
                                <Setter Property="Minimum" Value="0" />
                                <Setter Property="Maximum" Value="{Binding Inverter.Config.MaxAcPower, Converter={co:Multiply Factor=0.00144928}, FallbackValue=14.4928}" />
                                <Setter Property="Origin" Value="0" />
                                <Setter Property="UnitName" Value="A" />
                                <Setter Property="StringFormat" Value="N3" />
                                <Setter Property="GaugeColors" Value="{x:Static c:Gauge.HighIsBad}" />
                                <Setter Property="t:HalfCircleGauge.MinimumMaximumStringFormat" Value="N1" />
                            </Style>
                        </StackPanel.Resources>
                        <StackPanel Orientation="Horizontal">
                            <ContentControl>
                                <c:Gauge Value="{m:GaugeBinding Inverter.Sensors.Inverter.OutOfBalanceCurrentL12}" Label="L12" />
                            </ContentControl>

                            <ContentControl>
                                <c:Gauge Value="{m:GaugeBinding Inverter.Sensors.Inverter.OutOfBalanceCurrentL23}" Label="L23" />
                            </ContentControl>

                            <ContentControl>
                                <c:Gauge Value="{m:GaugeBinding Inverter.Sensors.Inverter.OutOfBalanceCurrentL31}" Label="L31" />
                            </ContentControl>
                        </StackPanel>
                    </StackPanel>
                </GroupBox>

            </WrapPanel>
            <!-- ReSharper restore Xaml.PossibleNullReferenceException -->
        </ScrollViewer>
    </Grid>
</c:ScalableWindow>