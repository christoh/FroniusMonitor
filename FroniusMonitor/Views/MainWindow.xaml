<Window x:Class="De.Hochstaetter.FroniusMonitor.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:De.Hochstaetter.FroniusMonitor.ViewModels"
        mc:Ignorable="d"
        Title="{x:Static p:Resources.AppName}" Height="810" Width="1440"
        d:DataContext="{d:DesignInstance Type=vm:MainViewModel}"
        xmlns:p="clr-namespace:De.Hochstaetter.Fronius.Localization;assembly=Fronius"
        xmlns:c="clr-namespace:De.Hochstaetter.FroniusMonitor.Controls"
        xmlns:images="clr-namespace:De.Hochstaetter.FroniusMonitor.Assets.Images">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" x:Name="PowerConsumerColumn"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Menu Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3">
            <MenuItem Header="{x:Static p:Resources.Settings}">
                <MenuItem Header="{x:Static p:Resources.Export}" Command="{Binding ExportSettingsCommand}"/>
                <MenuItem Header="{x:Static p:Resources.Load}" Command="{Binding LoadSettingsCommand}"/>
            </MenuItem>
            <MenuItem Header="{x:Static p:Resources.View}">
                <MenuItem Header="_Auto-fit" Click="OnAutoSizeChecked"/>
            </MenuItem>
        </Menu>

        <Viewbox Grid.Row="1" Grid.Column="0" Margin="16,16,0,16">
            <Grid Height="500">
                <Grid.RowDefinitions>
                    <RowDefinition Height="200" />
                    <RowDefinition Height="100" />
                    <RowDefinition Height="200" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <Grid Grid.Row="0" Grid.Column="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>


                    <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center">
                        <images:SolarPanel Width="80" Foreground="#004000" VerticalAlignment="Top" Margin="0,10,0,0" />

                        <c:PowerFlowArrow VerticalAlignment="Top" Fill="#ffd000" Margin="15,0"
                                          Power="{Binding SolarSystemService.SolarPowerAvg, FallbackValue={x:Null}}" />

                        <c:InverterControl Height="200" Inverter="{Binding SolarSystemService.SolarSystem.PrimaryInverter, FallbackValue={x:Null}}" />

                        <c:PowerFlowArrow x:Name="LoadArrow" VerticalAlignment="Top" Fill="LightGray" HasRightPlacement="True" DefaultsToOutgoing="True"
                                          Margin="25,0,10,0">
                            <c:PowerFlowArrow.ContextMenu>
                                <ContextMenu>
                                    <MenuItem Header="Include inverter power (Solar web mode)" IsChecked="{Binding IncludeInverterPower}" IsCheckable="True"/>
                                </ContextMenu>
                            </c:PowerFlowArrow.ContextMenu>
                        </c:PowerFlowArrow>

                        <images:House Width="80" Foreground="Black" VerticalAlignment="Top" Margin="0,15,0,0" />
                    </StackPanel>
                </Grid>

                <Grid Grid.Row="1" Grid.Column="0">
                    <StackPanel Orientation="Horizontal" Margin="100,-40,0,0">

                        <c:PowerFlowArrow Angle="-30" Fill="LightGray"
                                          Power="{Binding SolarSystemService.GridPowerAvg, FallbackValue={x:Null}}">
                        </c:PowerFlowArrow>

                        <c:PowerFlowArrow Angle="30" Margin="200,0,0,0" Fill="LightGreen" HasRightPlacement="True"
                                          Power="{Binding SolarSystemService.StoragePowerAvg, FallbackValue={x:Null}}" />

                    </StackPanel>
                </Grid>

                <Grid Grid.Column="0" Grid.Row="2" x:Name="LowerGrid">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <c:PowerMeterClassic Height="200" HorizontalAlignment="Left" Grid.Column="0" SmartMeter="{Binding SolarSystemService.SolarSystem.PrimaryMeter, FallbackValue={x:Null}}" />
                    <c:SmartMeterControl Height="200" HorizontalAlignment="Left" Grid.Column="1" Margin="10,0,0,0" SmartMeter="{Binding SolarSystemService.SolarSystem.PrimaryMeter, FallbackValue={x:Null}}" />
                    <ItemsControl Height="200" Grid.Column="2" DockPanel.Dock="Right" ItemsSource="{Binding SolarSystemService.SolarSystem.Storages, FallbackValue={x:Null}}" Margin="150,0,0,0">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal" IsItemsHost="True" HorizontalAlignment="Right" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <c:StorageControl Margin="0,0" Storage="{Binding}" />
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Grid>
            </Grid>
        </Viewbox>

        <GridSplitter Grid.Row="1" Grid.Column="1" ResizeBehavior="PreviousAndNext" BorderThickness="2" ResizeDirection="Columns"
                      BorderBrush="LightGray" Margin="10,0" VerticalAlignment="Stretch" />

        <ScrollViewer Grid.Row="1" Grid.Column="2"
                      VerticalScrollBarVisibility="Disabled" HorizontalScrollBarVisibility="Auto"
                      VerticalContentAlignment="Top" HorizontalContentAlignment="Stretch"
                      Margin="10,16,0,0" VerticalAlignment="Center" HorizontalAlignment="Stretch">
            <ItemsControl ItemsSource="{Binding SolarSystemService.SolarSystem.FritzBox.PowerConsumers, FallbackValue={x:Null}}">
                <ItemsControl.LayoutTransform>
                    <ScaleTransform x:Name="ConsumerScaler" ScaleX="1" ScaleY="1" />
                </ItemsControl.LayoutTransform>
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Vertical" IsItemsHost="True" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <c:PowerConsumer Width="200" Device="{Binding}" Margin="0,0,8,8" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Window>